[defaults]
# No static inventory — hosts are added dynamically via vars_prompt + add_host
inventory = localhost,
host_key_checking = false
stdout_callback = yaml
display_skipped_hosts = false
retry_files_enabled = false

[ssh_connection]
pipelining = true
