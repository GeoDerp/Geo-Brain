# Auto-generated by deploy.sh — DO NOT EDIT
# Source: authelia.access_control.policy labels in stack docker-compose files
access_control:
  default_policy: deny
  rules:
    - domain: "defectdojo.${DOMAIN}"
      policy: two_factor
    - domain: "dockge.${DOMAIN}"
      policy: two_factor
    - domain: "grafana.${DOMAIN}"
      policy: two_factor
    - domain: "home.${DOMAIN}"
      policy: one_factor
    - domain: "traefik.${DOMAIN}"
      policy: two_factor
    - domain: "moodle.${DOMAIN}"
      policy: one_factor
    - domain: "n8n.${DOMAIN}"
      policy: two_factor
    - domain: "notes.${DOMAIN}"
      policy: one_factor
    - domain: "wazuh.${DOMAIN}"
      policy: two_factor
