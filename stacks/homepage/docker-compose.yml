services:
  homepage:
    image: ghcr.io/gethomepage/homepage:v0.9.13
    container_name: homepage
    restart: always
    ports:
      - 3000:3000
    volumes:
      - ./config:/app/config:z
      - ${PODMAN_SOCK:-/run/user/1000/podman/podman.sock}:/var/run/docker.sock:ro
    networks:
      - mgmt-net
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.3'
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
    labels:
      - "security.stig.compliance=true"
      - "security.stig.role=dashboard"
      - "homepage.group=Management"
      - "homepage.name=Dashboard"
      - "homepage.icon=homepage.png"
      - "homepage.description=Service Dashboard"
      - "homepage.href=http://${DOMAIN}:3000"

networks:
  mgmt-net:
    # Use the same management network as Dockge for consistency
    external: true
