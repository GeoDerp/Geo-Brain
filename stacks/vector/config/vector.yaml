sources:
  journald:
    type: journald
  podman_logs:
    type: docker_logs

sinks:
  loki:
    type: loki
    inputs:
      - journald
      - podman_logs
    endpoint: http://loki:3100
    labels:
      host: "${HOSTNAME:-geo-brain}"
      source: vector
