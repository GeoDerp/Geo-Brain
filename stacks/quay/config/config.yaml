DB_URI: postgresql://${QUAY_DB_USER}:${QUAY_DB_PASSWORD}@quay-db:5432/${QUAY_DB_NAME}
SERVER_HOSTNAME: quay.${DOMAIN}
SETUP_COMPLETE: true
SUPER_USERS:
  - quayadmin
FEATURE_ANONYMOUS_ACCESS: false
FEATURE_USER_INITIALIZE: true
FEATURE_USER_CREATION: true
REGISTRY_TITLE: Geo-Brain Quay
REGISTRY_TITLE_SHORT: Quay
AUTHENTICATION_TYPE: Database
SECRET_KEY: GeoBrainQuaySecretKey123
DATABASE_SECRET_KEY: GeoBrainQuayDBSecretKey123

# Storage Configuration
DISTRIBUTED_STORAGE_CONFIG:
  default:
    - LocalStorage
    - storage_path: /datastorage/registry
DISTRIBUTED_STORAGE_DEFAULT_LOCATIONS:
  - default
DISTRIBUTED_STORAGE_PREFERENCE:
  - default

# Redis Configuration
BUILDLOGS_REDIS:
  host: quay-redis
  port: 6379
  password: ""
USER_EVENTS_REDIS:
  host: quay-redis
  port: 6379
  password: ""

# Clair v4 Integration
FEATURE_SECURITY_SCANNER: true
SECURITY_SCANNER_V4_ENDPOINT: http://clair:8080

# Pull-Through Proxy Cache (mirror docker.io, ghcr.io, quay.io)
FEATURE_PROXY_CACHE: true

# DISA STIG / Privacy
FEATURE_TELEMETRY_LOGGING: false
FEATURE_APP_SPECIFIC_TOKENS: true

# Kanidm OIDC SSO — DISABLED: Quay validates OIDC on startup via
# client_credentials grant, which Kanidm does not support (returns 401).
# The Kanidm client "quay" and secret (QUAY_OIDC_SECRET) are provisioned and
# ready; re-enable when Kanidm adds client_credentials grant support.
# FEATURE_DIRECT_LOGIN: true
# KANIDM_LOGIN_CONFIG:
#   CLIENT_ID: quay
#   CLIENT_SECRET: ${QUAY_OIDC_SECRET}
#   OIDC_SERVER: https://kanidm.${DOMAIN}/oauth2/openid/quay/
#   LOGIN_SCOPES:
#     - openid
#     - profile
#     - email
#   PREFERRED_USERNAME_CLAIM_NAME: preferred_username
#   SERVICE_NAME: Kanidm
